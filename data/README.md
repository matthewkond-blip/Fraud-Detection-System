Place your Kaggle-style `creditcard.csv` in this folder as `creditcard.csv`.

Alternatively, set the environment variable `FRAUD_DATA_CSV` to the full path of your CSV before running `python train.py`.
